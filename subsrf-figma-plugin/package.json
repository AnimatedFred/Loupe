{
  "name": "subsrf-figma-plugin",
  "private": true,
  "scripts": {
    "prepare": "node build.js",
    "build": "node build.js",
    "watch": "node build.js --watch",
    "prod":  "node build.js --prod"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
